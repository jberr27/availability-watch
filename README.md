# Availability Watch

A small, configuration-driven availability monitor. Target details and notification
credentials are supplied through repository secrets rather than committed to the code.

The workflow is designed to be triggered externally through `workflow_dispatch`.
